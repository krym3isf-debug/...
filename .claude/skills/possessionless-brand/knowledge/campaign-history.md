# Campaign History (append-only)

Historical Meta Ads tests and learnings go here, oldest to newest. This file is **append-only** — never delete or overwrite a past entry when new performance data comes in; that goes in `current-performance.md` instead. Move a period's data here once it's no longer "current."

Format for each entry:

```
## [Date range] — [Campaign/test name]
- What was tested:
- Result:
- Learning:
```

_No entries yet — populate from source material as it's provided._
